
import globe from '../../images/globe.png';
const Maps = (props) => {

    return (
        <div class="center">
        <img src={globe}/>
        <div class="welcome-text">Welcome To The World Data Mapper</div>
        </div>
    );
}

export default Maps;